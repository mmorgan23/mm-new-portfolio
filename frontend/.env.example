# Backend origin (no trailing slash). Production: https://your-api.up.railway.app
# Vercel: set this in Project → Environment Variables, then redeploy (required for live agent dashboard).
# Do NOT put CLAUDE_API_KEY here — only on Railway/Render with FastAPI.
VITE_API_URL=http://127.0.0.1:8000
